{
  "name": "@isocan/voice-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "//": "No runtime dependencies, deliberately: the page is markup, a controller and two modules that import nothing but browser globals. Everything below is a build or test tool. Nothing here is React, and that is the point of the package.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "jsdom": "^30.0.1",
    "vite": "^7.0.0",
    "vitest": "^3.0.5"
  }
}
